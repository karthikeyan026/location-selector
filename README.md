# React Location Selector

Simple React app with a three-level location picker for country, state, and district.

## Scripts

- `npm start` runs the development server.
- `npm run build` creates the production build.
- `npm test` runs the test command from `react-scripts`.
